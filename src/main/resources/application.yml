server:
  port: 8080
  servlet:
    context-path: /api
spring:
  application:
    name: srping-tutorial
  datasource:
    url: "jdbc:mysql://localhost:3306/gtel_spring1"
    driver-class-name: "com.mysql.cj.jdbc.Driver"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true